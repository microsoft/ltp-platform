Find benchmark results for workloads related to {workload} on hardware or sku most similar to the {design}, and only return the index in the json without any explanation. Perform the following steps:
Step 1. For each json item, identify the most similar hardware or sku to the {design} by comparing the 'hardware' and 'sku' fields.
Step 2. For all json items with an {design} in the 'hardware' or 'sku' field, to identify items whose "workload" field is VERY similar/consistent to {workload}. Note: dtoh and htod refer to GPU to CPU and CPU to GPU, respectively; 'nccl reduce performance' and 'nccl allreduce performance' are very different workloads; **"matmul" and "CUBLAS functions' performance" are very different workloads**; FP64, FP32, FP16 and FP8 are very different workloads.
Step 3. (This is very important!!!) Examine the selected json items to ENSURE that whose 'hardware' field or 'sku' field is VERY consistent to {design} and 'workload' field is VERY similar to {workload}. Otherwise, remove this item. If no json item is found, output None.
