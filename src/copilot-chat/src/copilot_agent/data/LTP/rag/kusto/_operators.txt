"To count entries in any table, use `| count`.",
"To filter data in a table, use `| where` followed by a condition. Example: `Table | where Column == 'Value'`.",
"To take a sample of N rows from a table, use `| take N`.",
"To define a time range, use `let` to assign start and end dates. Example: `let startdate = datetime(2025-03-20);`.",
"To filter rows based on a condition, use `| where`. Example: `Table | where Column == 'Value'`.",
"To filter rows within a time range, use `| where Column between (start .. end)`. Example: `| where PreciseTimeStamp between (startdate .. enddate)`.",
"To filter rows containing specific substrings, use `| where Column contains 'substring'`. Example: `| where hardwareGeneration contains 'AmdMI300X'`.",
"To filter rows matching multiple conditions, use logical operators like `or`. Example: `| where Tenant contains 'Value1' or Tenant contains 'Value2'`.",
"To get distinct values of specific columns, use `| distinct`. Example: `| distinct Column1, Column2`.",
"To join two tables, use `| join kind=<join_type>`. Example: `| join kind=rightouter (OtherTable) on KeyColumn`.",
"To create a new column, use `| extend`. Example: `| extend NewColumn = Column1 + Column2`.",
"To group data and calculate aggregates, use `| summarize`. Example: `| summarize count() by Column`.",
"To calculate the maximum value of a column, use `arg_max`. Example: `| summarize arg_max(Timestamp, *) by GroupColumn`.",
"To count rows matching a condition, use `countif`. Example: `| summarize countif(Column == 'Value')`.",
"To bin data into intervals, use `bin`. Example: `| extend Day = bin(Timestamp, 1d)`.",
"To calculate the day of the week, use `dayofweek`. Example: `| extend DayOfWeek = dayofweek(Timestamp)`.",
"To calculate the start of the week, use `startofweek`. Example: `| extend WeekStart = startofweek(Timestamp)`.",
"To format a datetime value, use `format_datetime`. Example: `| extend FormattedDate = format_datetime(Timestamp, 'yyyy-MM-dd')`.",
"To order rows, use `| order by`. Example: `| order by Column asc`.",
"To project specific columns, use `| project`. Example: `| project Column1, Column2`.",
"To remove specific columns, use `| project-away`. Example: `| project-away Column1, Column2`.",
"To calculate averages, use `avg`. Example: `| summarize AvgValue = avg(Column)`.",
"To round values, use `round`. Example: `| extend RoundedValue = round(Column, 2)`.",
"To replace null values, use `coalesce`. Example: `| extend NonNullValue = coalesce(Column, 0)`.",
"To create a range of values, use `range`. Example: `range Day from start to end step 1d`.",
"To expand arrays into rows, use `mv-expand`. Example: `| mv-expand ArrayColumn`.",
"To serialize rows for row-level operations, use `| serialize`.",
"To calculate cumulative sums, use `row_cumsum`. Example: `| extend CumulativeSum = row_cumsum(Column)`.",
"To calculate the difference between datetime values, use `datetime_diff`. Example: `| extend Difference = datetime_diff('day', Date1, Date2)`.",
"To pivot data, use `evaluate pivot`. Example: `| evaluate pivot(Column, sum(Value))`.",
"To filter rows with non-empty values, use `isnotempty`. Example: `| where isnotempty(Column)`.",
"To filter rows with empty values, use `isempty`. Example: `| where isempty(Column)`.",
"To calculate the week of the year, use `weekofyear`. Example: `| extend Week = weekofyear(Timestamp)`.",
"To calculate the minimum value of a column, use `minif`. Example: `| summarize MinValue = minif(Column, Condition)`.",
"To calculate the maximum value of a column, use `max`. Example: `| summarize MaxValue = max(Column)`.",
"To create conditional expressions, use `case`. Example: `| extend NewColumn = case(Condition1, Value1, Condition2, Value2, DefaultValue)`.",
"To concatenate strings, use `strcat`. Example: `| extend FullName = strcat(FirstName, ' ', LastName)`.",
"To trim characters from strings, use `trim`. Example: `| extend TrimmedValue = trim(' ', Column)`.",
"To summarize data with multiple aggregates, use `summarize` with multiple expressions. Example: `| summarize Count = count(), AvgValue = avg(Column)`.",
"To calculate row-level differences, use `prev` or `next`. Example: `| extend Difference = Column - prev(Column)`.",
"To calculate row-level durations, use `| extend Duration = Column2 - Column1`.",
"To calculate row-level averages, use `| summarize AvgValue = avg(Column)`.",
"To calculate row-level counts, use `| summarize Count = count()`."